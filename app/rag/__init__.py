# ======================================================================
# AI 模块包（#21）：RAG + LangGraph + MCP + 知识图谱 四大模块都在这里
# 作为技术中坚，我把它们按「职责单一」拆成独立文件，互相通过明确的函数签名协作：
#   embed.py   云端向量化客户端
#   llm.py     云端大模型客户端
#   chunking.py 板块内容切块（纯逻辑）
#   store.py   向量库(Chroma) + BM25(FTS5) 混合检索
#   retrieval.py RAG 管线：改写→混合召回→RRF→重排→压缩
#   graph.py   知识图谱（M4）：networkx 多跳 + 可视化
#   mcp_server.py 用 FastMCP 暴露 10 个工具
#   agent.py   LangGraph 单/多智能体（M2/M3）
#   api.py     AI 相关的 HTTP 路由（流式输出）
# =====================================================================
